# ── 隐私与生成物（永不入库；仅根目录，勿误伤 docs/ 下同名目录） ──
/journals/
/raw/
/reports/
/close/
*.bak
*.bat

# ── 外部工具与底座（本地保留，另行管理；仅根目录） ──
/tools/*
# 例外：银行导入需要的时区库（Windows 无 tzdata，deg 解析时区用；小体积、必须随仓库分发）
!/tools/zoneinfo.zip
/vendor/
/reference/
/archive/
/venv/
/paisa_test/

# ── Python ──
__pycache__/
*.py[cod]
.pytest_cache/

# ── Node（Paisa 构建产物） ──
node_modules/
dist/

# ── 系统/编辑器 ──
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── 本地敏感信息与机密配置（绝不入库） ──
docs/本地敏感信息_DO_NOT_COMMIT.md
config/local.yaml
config/actual_balances.yaml
docs/本地待办_DO_NOT_COMMIT.md
config/opening_balances.yaml
config/recurring_rules.yaml
config/opening_init.json
config/opening_draft.json

# ── AI 会话产物与机器证据（仅本地） ──
.planning/
docs/evidence/
收件箱/

# ── 加密备份（含敏感数据，永不入库；恢复流程见 docs/runbooks/03） ──
backups/
config/backup_secret.txt
