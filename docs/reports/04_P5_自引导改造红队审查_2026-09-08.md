---
title: P5 软件自引导改造红队审查报告
status: draft
last_reviewed: 2026-09-08
---

# P5 软件自引导改造红队审查报告

> 背景：用户否决"AI 代跑流水线"，要求把初始化与月度流程内置为软件自引导：
> 数据先不预置、由用户在软件内上传；用户不接触脚本/yaml。本报告记录改造与红队审查结果。
> 当前状态：实现与端到端自测完成，**红队审查中**（通过后翻 approved）。

## 1. 改造内容

| 项 | 内容 |
|---|---|
| 数据重置 | 真实账单归档 `journals/archive/`（账本仓 37fe938）；`raw/`、`paisa.db`、`reports/` 清空；Paisa 从空账本启动 |
| `finance.py status` | 新增 JSON 状态子命令（导入文件/期初/规则/check/FIXME/raw 文件），空账本安全 |
| `finance.py import` | 自动维护 `paisa_test/all.journal`（include 全量 journals），告别手工 include |
| Go 端点 ×4 | `GET /api/pcfo/init/status`；`POST /api/pcfo/upload`（multipart，文件类型白名单+密码入 local.yaml）；`POST /api/pcfo/init/opening`（校验科目/金额/日期→yaml→opening_entry）；`POST /api/pcfo/init/recurring`（校验→yaml→gen_recurring） |
| Svelte `/init` | 四步向导：①上传账单（多选+花呗密码+自动导入）②期初余额表单 ③定期规则表单（工资/房租/大件折旧）④状态总览（check/FIXME/同步到界面）；Navbar「初始化」入口 |
| 启动脚本 | `启动PersonalCFO.bat` 已修复双击乱码+端口问题（实测 6500 200） |

## 2. 端到端自测（实现方）

- 合成支付宝 CSV（真实前导 23 行 + 3 笔交易）经 upload 端点 → import 3 笔、FIXME 0、归因未解释 0、check OK
- opening 端点 → `2025-08-01-opening.journal` 生成、差额入 Equity:期初调整、check OK
- recurring 端点 → `recurring.journal`（工资/房租/折旧规则）生成、check OK
- status 全绿；all.journal 自动 include 三个 journal；/init 页面 200 + 截图验证（深色主题可读）
- 测试数据已清理，软件恢复空白初始态

## 3. 红队审查

### 一审（REPORT5.md，测试对象 25d9265）：不通过 —— P1×1、P2×7、无 P0

- **PASS**：a 空白态（零写操作）；b 注入/越界 33/34（路径穿越三类只落 basename）；c 端到端（合成 3 笔 0 FIXME、opening/recurring 生成、终态全绿）；e 月度复用（重传不双计、带密码花呗 PDF 拆分 100%）
- **P1-d FAIL**：opening/recurring 后 all.journal 不含 include（只有 import 路径刷新）→ Paisa 看不到期初/定期
- **P2×7**：`"1,2"` 去逗号按 12 接受；API 正数花呗记负债借方；GBK 控制台 forecast 崩溃；2025-02-30 类日期；years 小数；start 13 月；无有效文件仍写密码；paisa_test 残留杂物
- f/g/h/i 因回合中止 PARTIAL/SKIP

### 修复与自验证（主仓 beb24ab+536fd78，fork 835dec3+edc3957）

- P1-d：四个 journal 写入点（opening/gen_recurring/reconcile/sync）全部补 `refresh_paisa_includes()`；实测 init 后 all.journal 立即含 opening+recurring
- "1,2"：Go 校验原始串（1,2→400、1,234.56→ok）；Python `parse_money_strict` 同步覆盖四处
- 正数负债：Go 服务端自动转负（不依赖 UI）
- GBK 崩溃：finance.py 启动重配 stdout/stderr UTF-8（无 PYTHONIOENCODING 也 OK）
- 日期/年限/月：time.Parse 校验 2025-02-30→400；years 整数 1-60；start 01-12
- 密码顺序：有有效文件才写 local.yaml；paisa_test 杂物清理

### 终审：待红队复审结论（d 项 + "1,2" + 新校验），通过后回填并翻 approved。
