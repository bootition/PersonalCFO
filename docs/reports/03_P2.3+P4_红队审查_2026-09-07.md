---
title: P2.3+P4（sync/forecast/reconcile/SOP）红队审查与修复报告
status: approved
last_reviewed: 2026-09-07
---

# P2.3+P4 红队审查与修复报告

> 审查方式：独立子代理对抗验证（数字复算/合成偏移/边界攻击/SOP 走查）。
> 当前状态：首轮"有条件通过"→ 五项 P1 已修复回灌，**终审确认中**（通过后本文件翻 approved）。

## 1. 首轮逐项结论

- **P2.3 sync_fair_value**：PASS。C4 字段断言/未映射警告跳过/千分位/BOM/无调整路径/防覆盖/分录方向全部实测通过；落账后余额=CSV 市值、check 平衡。
- **P4.1 forecast**：PASS。现金合计、8 叶账户、13 个月、必要/总支出月均值、覆盖月数 6 组数字独立 hledger 复算分毫不差；1970 Open Balance 排除经真实代码路径合成实证。
- **P4.2 reconcile**：PASS。模板生成、合成 ±10 偏移与负债 +2.41 方向正确（写后重跑账实一致反证）、自动 check、同日分歧拒覆盖。
- **P4.3 SOP**：PASS。全部命令/路径/子命令真实存在。
- **双计复查**：PASS。journals/ 无 include 合并件；基金 76 条分录手工加总=hledger 余额（差 0.00）。

## 2. P1 五项与修复（已全部闭环）

| # | 问题 | 修复 |
|---|---|---|
| RT4-P1-1 | reconcile 未知科目静默按账面 0 → 错字凭空生成分录 | 新增 `hledger accounts` 白名单校验，未知科目警告并跳过 ✅ |
| RT4-P1-2 | reconcile yaml 未用 utf-8-sig（BOM 污染首行科目） | 改 utf-8-sig ✅ |
| RT4-P1-3 | sync 缺账户行/空市值单元格 → 市值静默按 0 → 全额减记无警告 | 空值逐行警告 + 整组缺失"若非清仓"警告 + 非法输入显式报错 ✅ |
| RT4-P1-4 | sync --date 不校验（非法日期毒 journal 瘫痪账本） | date.fromisoformat 校验拒绝 + --write 后自动 check ✅ |
| RT4-P1-5 | SOP include 刷新缺口（reconcile/fair-value 产物无提醒） | SOP 步骤 4 覆盖全部新 journal 类型+文件数自查；步骤 6 补 include 与季末再提交 ✅ |

## 3. P2 六项与处置

- forecast 除零/inf：已修（无必要支出显示 N/A）✅；`--forecast` period 参数越界生成问题：已改裸 `--forecast` + 显式 `-b/-e` 窗口 ✅
- reconcile 拒覆盖前误打"已写入"：已修（先拒后写）✅
- sync 非法金额/文件不存在裸 traceback：已改显式报错 ✅
- SOP 季末提交时序：已补提醒 ✅；SOP dry-run 环节：reconcile 默认即 dry-run（--write 才写），SOP 已按此顺序组织 ⏸
- 附带：红队注意到验证期间父代理并行开发（权益变动表、P1.5/1.6 生成器），已 diff 隔离验证无干扰——协作纪律实例。

## 4. 交付物

- 主仓修复提交：`d240899`（含五件套权益变动表补齐，2025/2026 跨年 19,001.93 衔接一致，恒等式校验 0.00）
- 终审跟踪项闭环提交：`25958d8`

## 5. 终审结论：**通过**（2026-09-07，REPORT4-final）

- 五项 P1 修复全部重跑实证有效；初审 P2 四项已修、一项部分解决。
- 终审追加的 4 个跟踪项（RT4F-1/2/3、RT4-P2-3）已全部闭环并复测：Expenses 类科目穿透被拦、含空格科目名保全、inf/nan 解析期拦截、写败回滚、forecast 空窗文案。
