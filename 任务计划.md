# PersonalCFO · 任务计划（交接版）

> 本文档是**新开对话接续开发**的唯一入口。读法：先读 `docs/STATUS.md`（当前状态唯一权威）与本文件，再读 `docs/decisions/01_架构总纲_v3定稿.md`（架构与全部决策），然后按阶段执行。每阶段完成一条勾一条，并更新文末"执行日志"。勾选前须通过红队审查（制度见 `AGENTS.md`）。

## 0. 项目一句话

**PersonalCFO**：把个人当公司来经营的本地财务系统——hledger 复式账本为唯一真相，deg 导入支付宝/微信/花呗/银行卡账单，Wealthfolio 承担投资子账本与目标规划，Paisa（深改）做统一日常 UI，按上市公司节奏出四份财报（PDF/CSV）。

## 1. 目标与非目标

**目标（v1）**
1. 账单→复式账全自动：支付宝 CSV、微信 XLSX、花呗月账单（PDF 拆分组合支付）、银行卡明细（建行/工行/招行/光大）全部进入 hledger journal。
2. 权责发生制：工资月末计提+到账冲销（规则先建好，就业后生效）；房租预付按月摊销；固定资产月度折旧。
3. 四期财报：Q1/H1/Q3/年报累计口径，五件套（资产负债表、利润表、现金流量表、含权益资产负债表、权益变动体现）+ 指标摘要（规模结构/盈利增长/现金结余/**应急金覆盖月数**）+ 现金流预测，PDF+CSV。
4. 投资子账本：Wealthfolio 记投资逐笔，季度回写公允价值调整分录到 journal，财报投资数字=仪表盘数字。
5. 日常 UI：Paisa Fork（修 bug、中文化、Wealthfolio 风格、挂入口、账单日历），查账/编辑/FIXME 不开命令行。
6. 运维：git 私库（账本独立仓库）、月度账实核对清单。

**非目标（v1 明确不做）**
预算（先观察 1-2 季度再定）、Markdown/Obsidian 归档、税务参考、AI 本地预分类、手机端、目标储蓄专用科目（目标在 Wealthfolio 侧）、Connect 订阅、多人/云。

## 2. 架构与数据流（详见 v3 定稿附录 A）

四组件介入深度：hledger/deg/Wealthfolio **原样使用**（只写配置/录数据），Paisa **Fork 改造**，finance.py 等 **自写胶水**。咬合点是文件契约（journal/YAML/CSV/PDF），不是代码。八条数据流 F1-F8 见 v3 定稿第 4 节。

## 3. 当前工作区盘点（已有什么）

| 资产 | 位置 | 状态 |
|---|---|---|
| deg v2.15.1 | `tools/double-entry-generator.exe` | ✅ 实测支付宝/微信/自造微信 xlsx 全链路通过 |
| hledger 1.52.3 | `tools/hledger-bin/hledger.exe` | ✅ 五表/结账/预测/公允价值实测通过 |
| paisa CLI v0.7.6 | `tools/paisa-cli.exe` | ✅ 可运行；hledger 后端有 1 行 bug |
| Paisa Fork 工作副本 | `vendor/paisa`（已打补丁 `internal/ledger/ledger.go:205`） | ✅ 补丁已改，**未构建**（需先 npm 构建 web 资产） |
| deg 规则初版 | `config/alipay.yaml`、`config/wechat.yaml` | ✅ 按真实账单统计编写，待迭代收敛 |
| 科目表草案 | `config/科目表草案.md` | ✅ 含期初建账模板 |
| 编排器原型 | `src/finance.py` | ✅ check/report/close/reopen 已跑通 |
| 演示账本+财报 | `journals/2026.journal`、`reports/demo2026/` | ✅ 四期五件套 PDF 演示包 |
| 花呗账单样例 | 路径与密码见本地文件 `docs/本地敏感信息_DO_NOT_COMMIT.md`（不入库） | ✅ 格式已验证可用（附录 B） |
| 真实账单 | 支付宝/微信账单位置见本地文件 `docs/本地敏感信息_DO_NOT_COMMIT.md`（不入库） | ✅ 支付宝 2017 行、微信 349 行（2025-08~2026-07） |
| 调研档案 | `docs/research/`、`archive/`、`reference/` | ✅ 24 候选对比、实测证据 |

## 4. 实施计划

### P0 仓库与环境（0.5 天）
- [x] 0.1 `git init` + 本文件/代码/文档首次提交（本地已有的话确认干净）。
- [x] 0.2 GitHub 建私有仓库 `PersonalCFO`（代码仓）推送（✅ 已完成 2026-09-06）；账本私仓 `PersonalCFO-ledger` **暂缓**（用户决定：账本先放本地，本地 git 已初始化，备份方案以后再研究）。
- [x] 0.3 确认 `.gitignore` 生效：`git status` 不得出现 journals/ raw/ reports/ tools/ vendor/ reference/ archive/。
- [x] 0.4 若需 Python 包，重建 venv：`python -m venv venv && venv/Scripts/python -m pip install pypdf pdfplumber`（beancount/fava 仅调研用，可不装）。
- 验收：`git status` 干净；`python src/finance.py check` 通过。

### P1 会计核心上线（2-4 天，最大工作量）
- [x] 1.1 **账单导入流水线** `src/finance.py import`：raw/ 放支付宝 CSV、微信 XLSX、银行卡明细 → 逐平台调 deg → `journals/import-*.journal`；每平台写冒烟测试（行数、科目分布、FIXME 计数）。
- [x] 1.2 **花呗拆分** 新脚本 `src/split_huabei.py`：
  - 输入：花呗 PDF（pypdf+密码）+ deg 生成的 journal；
  - 匹配：美团 26 位单号 > 日期+金额+名称；两级匹配，未匹配输出 FIXME 清单；
  - 输出：组合支付拆成两条分录（花呗承担→`Liabilities:花呗`，其余→对应科目）；`自动还款`→`借:花呗/贷:银行`；`退款还款`→冲销分录；
  - 验收：样例 PDF 全量匹配率 ≥95%，拆后 `hledger check` 平衡。
- [x] 1.3 **deg 规则迭代**：对真实 2025-08~2026-07 账单跑全量，按 FIXME 清单逐类补规则（目标 FIXME 金额占比 <5%）。
- [ ] 1.4 **银行导入（⏸ 暂缓，待数据）**：用户决定先不导出银行明细。过渡方案 = **银行卡半追踪模式**：贷方变化靠支付宝/微信账单的"支付方式=XX储蓄卡"推断入账；期末手动盘点真实余额，差异出调账分录（见 4.2 reconcile）。**待拿到各银行导出明细后**再补 `config/ccb.yaml` 等升级为全自动导入（deg providers：ccb/icbc/cmb/ceb 已存在，按真实格式调规则）。
- [x] 1.5 **期初建账**：盘点支付宝/余额宝/微信/零钱通/各银行卡/花呗/投资/固定资产当前余额，写一条期初分录（模板 `config/科目表草案.md`），差额进 `Equity:期初调整`。 ✅ 软件自引导（2026-09-08）：用户在 Paisa「初始化」页第 ② 步表单填数 → 端点自动写 `config/opening_balances.yaml` → 自动跑 `opening_entry.py` → check
- [x] 1.6 **定期规则**：`~ monthly` 工资计提（金额先占位，就业后改数）、折旧、房租摊销；报表命令加 `--forecast`。 ✅ 软件自引导（2026-09-08）：用户在「初始化」第 ③ 步填工资/房租/大件折旧 → 自动生成 `recurring.journal` → forecast 报表即时生效
- [ ] 1.7 **历史重导**：2025-08~2026-07 全量导入，跑 `python src/finance.py report 2025` 与 `report 2026` 对照。
- 验收：四期财报在真实数据上首跑；每期五表数字自洽（资产=负债+权益）；FIXME 清单可读。

### P2 Wealthfolio 接入（1 天）
- [ ] 2.1 安装官方 Windows 版；建 4 个投资账户（哈利布朗/攒股收息/虚拟货币/CS饰品），Transactions 模式录期初持仓与历史交易。
- [ ] 2.2 配置目标（应急金、攒股目标）与再平衡阈值；FIRE 模块不管（白送）。
- [x] 2.3 新脚本 `src/sync_fair_value.py`：读 Wealthfolio holdings 导出（CSV/REST）→ 对比 journal 投资成本 → 生成季度调整分录（借/贷投资科目、贷/借 `Income:投资损益:公允价值`）；字段名做兼容断言。
- 验收：季度回写分录平衡；财报投资科目 = Wealthfolio 期末市值。

### P3 Paisa Fork（2-3 天）
- [x] 3.1 构建：`cd vendor/paisa && npm install && npm run build`（产出 web 资产）→ `go build`（GOPROXY 用 `https://goproxy.cn,direct`）→ 验证 `paisa update` 不再 FATAL。
- [x] 3.2 中文化：找 i18n 机制（docs/blog/posts/localization.md），加 zh-CN 词条。
- [x] 3.3 主题：把 Tailwind 设计令牌切成 Wealthfolio 风格（参考 `reference/wealthfolio/apps/frontend` 的配色/字体/卡片）。
- [x] 3.4 入口：顶栏加"导入/财报/结账/对账"按钮（调 `src/finance.py`）；启用账单/还款日历；预算页面隐藏（二期）。
- [x] 3.5 打包 Windows 可用形态（dev 先行，后续再打包）。
- 验收：日常查流水/编辑/FIXME/看日历全部在 UI 完成；界面美观统一。

### P4 打磨（持续）
- [x] 4.1 `finance.py forecast`：现金流预测报表（`--forecast=未来12个月`）+ 应急金覆盖月数进指标摘要。
- [x] 4.2 `finance.py reconcile`：账面余额 vs 手动实录 → 差异清单 → 调账分录向导。
- [x] 4.3 月度 SOP：导入（F1/F2/F3）→ FIXME 清零 → 对账 → （季末）回写投资+结账+出财报，目标 30 分钟内完成。
- [ ] 4.4 观察 1-2 季度后：评估是否启用预算（Paisa 内置）、AI 预分类（抄 Beancount-Trans BERT）、手机端（tailscale）。

## 5. 命令速查

```bash
venv/Scripts/python src/finance.py check            # 账本平衡校验
venv/Scripts/python src/finance.py import           # 导入 raw/ 账单（自动链式花呗拆分+冒烟+归因）
venv/Scripts/python src/finance.py report 2026      # 生成四期财报 PDF/CSV
venv/Scripts/python src/finance.py close 2026 Q1    # 结账
venv/Scripts/python src/finance.py reopen 2026 Q1   # 反结账
venv/Scripts/python src/finance.py forecast         # 现金流预测 + 应急金覆盖月数
venv/Scripts/python src/finance.py reconcile        # 账实核对（--write 生成调账分录）
venv/Scripts/python src/split_huabei.py journals/import-alipay-*.journal   # 手动花呗拆分（一般经 import 自动调起）
venv/Scripts/python src/sync_fair_value.py raw/<holdings>.csv --date <季末日> --write  # 投资公允价值季度回写
tools/hledger-bin/hledger.exe -f journals/import-*.journal balance        # 快速查余额
tools/hledger-bin/hledger.exe -f journals/import-*.journal register FIXME # FIXME 待确认清单
vendor/paisa/paisa.exe update --config paisa_test/paisa.yaml   # Paisa 同步（hledger 须在 PATH）
vendor/paisa/paisa.exe serve --config paisa_test/paisa.yaml    # Paisa 日常 UI（或双击 启动PersonalCFO.bat）
taskkill //IM paisa.exe //F                        # 停止 Paisa（Git Bash pkill 无效）
```

## 6. 风险与红线

1. **隐私红线**：`journals/`、`raw/`、花呗 PDF、任何含身份证/卡号内容**永不进主仓库、永不外发**；账本单独私库。提交前 `git status` 自查。
2. **Paisa 构建**：web 资产未随源码发布，必须 `npm run build` 后再 `go build`，否则 `web/web.go: pattern all:static: no matching files found`。
3. **Windows 编码**：hledger 一律走 finance.py（自动 chcp 65001）；手写命令先 `chcp 65001`。
4. **deg 规则顺序**：method 规则后命中覆盖前命中；`fullMatch` 防"余额"误伤"余额宝"。
5. **Wealthfolio 导出字段**：回写脚本对 holdings CSV 字段名断言，不符时报错而非猜。
6. **许可证**：自用无碍；分发修改版（尤其 Paisa/Wealthfolio AGPL）需开源相应部分。

## 7. 执行日志

- 2026-09-06：工作区重命名为 PersonalCFO；调研产物归档至 `archive/`、源码至 `reference/`、Paisa 至 `vendor/`；原型迁入 `src|config|journals|reports`；本计划成文；主仓推送至 bootition/PersonalCFO；journals 本地 git 初始化。
- 2026-09-06（决策变更）：账本私仓 `PersonalCFO-ledger` 暂缓（本地 git 先用，备份以后再研究）；银行账单导入暂缓，P1.4 改为"银行卡半追踪模式"，拿到明细后再升级为全自动。
- 2026-09-06（隐私清理）：从主仓移除花呗账单路径/密码与账单目录路径，统一收拢到本地 `docs/本地敏感信息_DO_NOT_COMMIT.md`（.gitignore 排除），并 amend 重写首次提交后强推，GitHub 历史不再含敏感信息。
- 2026-09-06（P0 完成）：0.1 主仓 git 干净且与 origin/main 同步；0.3 `git check-ignore` 抽查 journals/raw/reports/tools/vendor/reference/archive/close 及本地敏感信息文件全部被正确忽略，`git status --porcelain` 为空；0.4 重建 venv（Python 3.14.2，pypdf 6.17.0 + pdfplumber 0.11.10）；验收通过：`venv/Scripts/python src/finance.py check` 输出 "check OK 账本平衡"（exit 0），`git status` 干净。→ 进入 P1。
- 2026-09-06（治理基线）：按 docs-git-governance skill 建立 Diátaxis 文档布局 + `docs/STATUS.md` 唯一真相 + `AGENTS.md`（红队审查制度：每阶段完成后独立子代理对抗验证，通过才勾选）；修复 skill 校验脚本两处 bug（本仓 `scripts/` 副本 + 全局 skill 已同步修）。
- 2026-09-06（P1.1-1.3 完成，红队复审通过）：①deg 语义源码级确认（顺序评估/后命中覆盖/方向规则/ignore），双平台配置分层重写，删除吞掉一切的兜底规则；支付宝 FIXME 金额占比 4.06%（笔数 15.07%）、微信 16.71%（笔数 13.75%），残余=用户定性队列（单挑啊 12.2k/校园卡充值 5.0k/邱景衡 3.7k/邱鸿自转 4.6k/红包 1.5k 等，清单待用户批处理）。②红队首轮抓出 2 个 P0（收益发放方向反转 447 笔、deg 吞首行）+归因错误+口径问题，全部修复（L6 矫正/献祭行/orderId 集合精确归因未解释=0/双口径）。③split_huabei.py 五级匹配+分期聚合，匹配率 97.36%（≥95%），组合拆分 54 笔，拆后 check 平衡；幂等护栏+还款池收紧已加固。详见 `docs/reports/01_P1.1-1.3_红队审查_2026-09-06.md`。
- 2026-09-06（P1.7 部分）：演示账本归档 `journals/archive/`；真实 2025-08~2026-07 全量导入（支付宝 1957 笔+微信 320 笔，账本仓已提交）；`report 2025`/`report 2026` 四期五表 PDF 首跑完成；**数字自洽（资产=负债+权益）待 1.5 期初建账（用户盘点）后复核**。
- 2026-09-06（决策补充）：用户指示——需人工参与项（1.5 期初建账、1.6 定期规则金额、P2.1/2.2 Wealthfolio 安装录入、FIXME 定性队列）先跳过，全部完成后统一处理。
- 2026-09-07（P3.1 完成）：`vendor/paisa` npm install + vite build（web 资产→web/static）+ go build（GOPROXY=goproxy.cn）成功；接真实 journal（all.journal include）`paisa update` 入库 4614 postings 不再 FATAL。**新发现并修复更大 bug**：utils.Exec 的 CREATE_NO_WINDOW 使 hledger 回退 CP936 读 UTF-8 journal 必崩（exit 1），改为继承控制台+SetConsoleCP(65001)；连同既有 ledger.go:205 补丁，两则修复已提交 vendor/paisa 本地仓（b9f0f7d）。
- 2026-09-07（P2.3/P4.1/4.2/4.3 实施，待红队审查后勾选）：sync_fair_value.py（C4 字段断言+合成 CSV 测试；顺带抓到 all.journal 与 import journal 双计问题，合并文件已移出 journals/）；finance.py 新增 forecast（现金/月均必要支出/应急金覆盖月数，期初前口径已标注）与 reconcile（模板→差异→调账分录→check 闭环）；docs/runbooks/01_月度SOP.md 成文（30 分钟闭环+排错速查）。
- 2026-09-07（P3.2-3.5 实施，红队审查中）：3.2 中文化=直改 Svelte 字符串（Paisa 无 UI 语言 i18n，localization 仅数字格式）：导航 36 标签+主面板+现金流图例/提示（80/20，深层页面留待下迭代）；3.3 主题=Bulma 变量层换 Flexoki 令牌（paper/#282726 主按钮/#205ea6 链接/圆角 0.75rem/Inter+JetBrains Mono），Edge headless 截图验证浅/深两色；3.4 入口=`POST /api/pcfo/run` 白名单端点 + /cfo 工具页 + 预算隐藏，curl 端到端通过；3.5 dev 形态=启动PersonalCFO.bat（不入库）。fork 提交 9a36280（vendor/paisa 本地仓）。
- 2026-09-07（P3 附注）：发现并修复比 ledger.go:205 更大的 CREATE_NO_WINDOW 编码 bug（hledger 无控制台回退 CP936 读 UTF-8 必崩），修复方式=继承控制台+SetConsoleCP(65001)；另实锤 Git Bash pkill 杀不了 Windows 进程，运维须用 `taskkill //IM paisa.exe //F`（已写入 SOP 排错速查）。
- 2026-09-07（用户待办一键化）：新增 `src/opening_entry.py`（P1.5：模板→填数→期初分录+平衡校验，防重复建账）与 `src/gen_recurring.py`（P1.6：模板→填数→`~ monthly` 计提/摊销/折旧规则，折旧次月起限期内直线，--force 重建）；两个 yaml 模板均 gitignored，合成数据测试通过后已清理测试痕迹。用户后续只需"填数+跑一条命令"。
- 2026-09-08（软件自引导改造 P5）：原"AI 代跑流水线"被否决（用户反馈"应该写入软件作为初始化流程"）。已做：①真实账单归档 journals/archive/、清空 raw/、删 paisa.db、恢复干净初始态；②`finance.py status` 新增（JSON：导入/期初/规则/check/FIXME，空账本安全）；③Go 端点 4 个：`GET /api/pcfo/init/status` / `POST /api/pcfo/upload`（multipart 账单+花呗密码）/`POST /api/pcfo/init/opening`（JSON→yaml→opening_entry）/`POST /api/pcfo/init/recurring`（JSON→yaml→gen_recurring）；④`finance.py import` 自动维护 paisa_test/all.journal（告别手工 include）；⑤Svelte `/init` 向导页（4 步 tab：上传/期初/规则/状态总览）+ 空账本主页首选引导；⑥端到端自测通过。P1.5/1.6 勾选（现由 UI 完成）。
- 2026-09-08（P5 红队审查关闭，通过）：一审发现 P1×1（opening/recurring 后 all.journal 不刷新——四处 journal 写入口已补）+ P2×8（"1,2"金额/正数负债/GBK 控制台崩溃/2025-02-30/years 小数/start 13 月/无文件写密码/负数工资房租折旧）；终审三复审点实测 PASS，残留负金额已补修（fork f024a85）。详见 `docs/reports/04_P5_自引导改造红队审查_2026-09-08.md`。软件当前处于**空白初始态**：用户双击 bat → Paisa「初始化」向导走 ①②③④ 完成首装，以后每月回「① 上传账单」。
- 2026-09-09（P6 用户反馈修复）：①上传交互改为拖拽区+已选 chips（可逐个删）+点选追加（不会覆盖），支持多文件一次传 3 份（实测 OK）；②Paisa UI 改为 Wealthfolio 风格固定左栏 232px（竖排导航+可展开子项+纸面+圆角+暗色兼容）。paisa.yaml 改绝对路径（相对路径 update 失败），bat 自愈用 %~dp0 展开纯 ASCII（避免中文乱码）。fork 49de1fe。
- 2026-09-09（P6 红队终审 PASS，收官）：独立红队全项实测通过（a 多文件上传 / b 拖拽追加 / c 侧边栏桌面+移动+深浅色 / d active 自动展开 / e 上下文控制栏 / f bat 自愈两轮 / g 隐私卫生 / h 自由攻击）。P1×1：49de1fe 移动端汉堡按钮藏在 `translateX(-100%)` 的 aside 内（移动端无导航入口）→ 审查期间修复 fork `bbb30ad`（按钮移出 aside、fixed top-left），重建 exe 复测关闭。P2×3 不阻断（/cash_flow 裸路径 404、上传无显式大小上限、既有 tippy allowHTML）已回写 STATUS 缺口，排入后续打磨。详见 `docs/reports/05_P6_多文件上传_Wealthfolio侧边栏红队审查_2026-09-09.md`（approved）。
- 2026-09-09（P7 UI 整页重做 + 数据库重置，🟡 进行中）：①**清空运行态**：`src/reset_init.py`（新）把 raw/* 与 journals/import-*.journal 归档到 `journals/bak/<ts>/`（非销毁），删除 `paisa_test/paisa.db(+sidecars)`、重置 `paisa_test/all.journal`、删除 `config/local.yaml`；`finance.py status` 全空、check_ok。多文件上传 P6 已修，用户现在可双击 bat 重走 `/init` ①②③④。②**根因修图表黑底**：`textures.lines().background(...)` 在 `svg.call(texture)` 时会在整张 svg 底铺一块矩形（深色主题下变黑底，浅色下变彩色块）→ 改为只 `svg.append("defs").html(texture.toString())` 注入 pattern defs，bar 仍是花纹但不盖整张 svg；并把图案背景设为 `rgba(0,0,0,0)`。③**Flexoki 令牌**：`src/app.scss` 顶部新增 `:root` 完整色板（paper / base-50..950 / black / accent / surface / text / grid / positive/negative），`html[data-theme="dark"]` 覆盖深色面；body 去掉 dot-grid 背景；`.box` 走 surface+border+0.75rem；`.axis` 轴/网格/文字用令牌；侧栏 hex 全部换成 `var(--…)`，active 态走 `--color-accent-soft`。④**侧栏加图标**：Navbar.svelte 给每个顶级组（现金流/支出/资产/负债/收入/账本/初始化/CFO 工具/更多/税务/关于）加 FA 图标，渲染 `<span class="pcfo-nav-icon"><i class="fas …"/></span>`。⑤重建：`npm run build`（39.8s） + `go build`（53MB paisa.exe, 16:51）成功；boot `/api/pcfo/init/status` 200，资产中含 `--color-paper` 与 `pcfo-nav-icon`。
- 2026-09-09（P7 第二轮：组件皮肤 + 向导/工具页重做 + 深色兜底）：①**accent 色板补齐**：从 `reference/wealthfolio globals.css` 抽取 red/orange/yellow/green/cyan/blue/purple/magenta 8 色 × 50–950 共 104 个 `--color-*-*` 令牌，语义令牌映射 accent=blue-600、positive=green-600、negative=red-600。②**组件皮肤**：`.button`（is-link=accent 底/is-primary=黑底纸字/is-light=surface-2）/`.input`/`.textarea`/`.select`（surface+hairline+0.75rem+accent focus ring）/`.tag`/`.table`/`.notification`/`.tabs` 全部令牌化。③**`/init` 重做**：胶囊步骤条（编号圆点+完成态绿边+active accent 底），步骤面板 `.pcfo-card`，上传区 `.pcfo-upload-zone`（hover/拖入 accent 反馈）、文件 chips（圆角胶囊+单删按钮），期初余额 `.pcfo-form-grid` 网格，大件折旧 `.pcfo-asset-row` 四列+删除，状态总览 `.pcfo-status-grid` 六张统计卡；多文件/拖拽/追加逻辑保持 P6 不变。④**`/cfo` 重做**：四个工具卡（accent icon tile+标题+说明+按钮）`.pcfo-card-grid`，年份/期间+财报/结账/反结账进 `.pcfo-report-bar`。⑤**深色兜底**：dark.scss 末尾 `!important` 三条（body/.pcfo-sidebar+*/sidebar/brand-logo）解决嵌套 @import 把 body 背景重写成纸面的级联 bug；Edge headless 实测 body=#100f0f、sidebar=#1c1b1a、card=#1c1b1a、step-active=#205ea6，无 console error。⑥重建：`npm run build` 28.6s + `go build`（paisa.exe 17:03）。**下一轮：派独立红队 a–h + 自由攻击复审 → 出 docs/reports/06。**
- 2026-09-09（P7 红队终审关闭，通过）：独立红队 REPORT7 一审**不通过（有条件）**：a Flexoki 令牌 PASS、b 图表去黑底 PASS（静态判定）、c 侧边栏 PASS、d 组件皮肤 PASS、e /init PASS（结构+API）、f /cfo PASS、g 深色一致性 PASS、i 隐私卫生 PASS、j 自由攻击 PASS、k 构建一致性 PASS；**h 多文件上传的 check_ok 子项 FAIL**——根因 P1-1：`finance.py status()` 裸 `subprocess.run` 未走 UTF-8 通道，经 Paisa piped stdio 调 hledger 按 CP936 解码中文 journal → 步骤④报 ❌。**已修**：`status()` 改走 `run_console_utf8`（与 check() 同路径），中文 journal 复测 check_ok:true/exit 0。P1-2：`/init` 同步按钮发 `{}` 空跑（fork `a8d1ebd` 改 `{"journal":true}`，红队 API 级复测通过）。P2×4：LegendCard 图例 swatch 残留 texture；reset_init.py 卫生缺口（recurring_rules.yaml 名不符/opening+recurring journal 残留/0.3s 文件锁）→ **本轮已修**；继承项（裸 /cash_flow 404、上传无硬上限、tippy allowHTML）与 deg 吞行观察项回写 STATUS 缺口。`docs/reports/06_P7_UI整页Wealthfolio化红队审查_2026-09-09.md` 已翻 approved。软件处于空白初始态：双击 bat → `/init` ①②③④。
- 2026-09-09（P7.1 用户反馈修复，fork `4401bc2`）：①**默认浅色**：主题默认 paper 浅色并换新 storage key 重置旧的系统深色偏好，移除 `prefers-color-scheme` 跟随；`app.html` 初始 `data-theme="light"`、title 改 PersonalCFO。②**空账本首页全中文化**：删除英文列表与 Setup Demo（“点击后跳回同页”的入口连同问题一起移除），改单一「欢迎使用 PersonalCFO + 开始初始化」CTA 直达 `/init`。③**/init 步骤条**：一排平行圆点胶囊改为横向分段控件（①-④ 文字 + 完成勾，移动端 2×2）。Edge headless 实测：theme=light、body #fffcf0、无 Setup Demo、steps=4、num-dots=0、console 0 错；svelte-check 0 错；`paisa.exe` 20:40 重建。
- 2026-09-09（P7.2 期初统计重做，按用户方案）：①**自动跳转**：上传账单成功 → 自动进第②步（成功提示挂在输出区）。②**科目推导**：`finance.py status()` 新增 `balance_accounts`（只列 Assets:现金/银行存款、Liabilities:*，排除 FIXME；投资/固定资产不混入）；向导第②步动态生成字段，无账单时用 6 个通用兜底科目，删除写死的 CS饰品/电脑等个人科目。③**投资逐项**：每项=名称/类别/代码/可选当前市值 + 多笔买入（日期+数量+单价）；成本=建账日及之前 Σ数量×单价；市值-成本差额自动进 Equity:期初调整（历史盈亏不进当期收益）。④**固定资产逐项**：名称/类别/原价/购买日期/年限/可选残值；自动追溯累计折旧（直线法，按完整月数，残值封底），期初记净值，并生成 `journals/fixed-assets-recurring.journal`（`~ monthly from … to …`）按月继续折剩余部分。⑤**后端 JSON 模式**：`opening_entry.py --json config/opening_init.json`（新增计算+写文件+check）；Go `/api/pcfo/init/opening` 改收 investments/fixed_assets 并严格校验（名称 40 字符/无冒号、日期真实存在、数量 `^\d+(\.\d+)?$`、金额仅 123 或合法千分位、年限 1-60）；`.gitignore` 增加 `config/opening_init.json`。⑥**自测**：Python 单元验证（支付宝 100/花呗-50/沪深300 成本 3500 市值 4300→资产 4300 差 800 入期初调整；MacBook 2023-06 购 12000 三年→已折旧 8666.67/净值 3333.33/剩余规则 2025-09~2026-06 月 333.33）；Playwright 截获 opening POST 验证 payload 与自动进③；svelte-check 0 错；`paisa.exe` 21:17 重建。
- 2026-09-09（P7.3 初始化守卫 + 清空重测）：①**根因**：bat 启动会跑 `paisa update`，把 journals 同步进 DB，主页只用“有没有交易”判断，于是“账单已导入但期初未建”也会被放行进主页面。②**守卫**：主页 onMount 查 `/api/pcfo/init/status`，`imported>0 && opening=false` 立即 `goto("/init")`（replaceState），并渲染“初始化未完成”横幅兜底；第④步加「去总览」。③**清空重测**：运行 `reset_init.py`（用户 20:51 上传的真实 import journal 与 raw 全部归档到 journals/bak/20260909-214358 与 -214612，paisa.db/all.journal 复位），`finance.py status` 全空。④**验证**：Playwright 空白态留欢迎页；模拟 imported>0/opening=false 自动跳 /init；去总览按钮存在；svelte-check 0 错；`paisa.exe` 21:45 重建。
- 2026-09-09（P7.4 期初表单体验修复，fork `87ba6c2`）：①**科目标签具体化**：`accountLabel` 改按科目末段生成——`Assets:银行存款:建设银行`→「建设银行银行卡」、`Assets:现金:余额宝`→「余额宝余额」、「花呗」→「花呗待还」（此前四个银行字段都叫“银行存款”，用户无法区分）。②**建账日期默认今天且可编辑**：记不清历史余额就填现在的数；文案说明“从此刻起 资产=负债+权益 准确，历史期间资产负债表有偏差（利润表不受影响）”。③**「其他科目」自定义行**：科目名（Assets:/Liabilities: 开头，前端校验）+ 金额，可添加纸币等电子账单无法体现的资产；负债自动转负。④**进页即拉状态**：onMount 与点第②步都 refreshStatus，直接跳第②步也显示账单推导科目。⑤**验证**：Playwright 实测标签具体化、日期默认今天/可改/入参正确、纸币自定义行进入提交 payload；svelte-check 0 错；`paisa.exe` 22:18 重建。用户指示：数据库**不再**清空，直接跳第②步继续。
- 2026-09-10（P7.5 守卫跳转崩页修复，fork `0409d2b`）：用户双击 bat 报 `TypeError: Cannot read properties of null (reading 'parentElement')`（monthly.js → 主页）。根因：P7.3 初始化守卫在 onMount 里 `await goto("/init")` 之后没有 `return`，SvelteKit 已卸载主页组件，后续 d3 图表渲染仍执行 `document.getElementById(...).parentElement` → null 崩溃。修复：跳转后立即 `return`。验证：真实数据（imported 2 journals + opening false）→ `/` 自动跳 `/init`，Playwright 捕获 0 console/page error；`/init` 页 0 错；svelte-check 0 错；`paisa.exe` 20:58 重建。注：此前 P7.3/P7.4 的 main docs 提交 `e87c479→ff86b84` 因 GitHub 连接重置未推送，待网络恢复补推。
- 2026-09-10（P7.6 期初录入体验 6 项，fork `af20aa7` + 合并补丁，用户指示不清库）：①**分批录入**：新增 `GET/POST /api/pcfo/init/opening/draft`（写 `config/opening_draft.json`，UTF-8 校验），表单加「保存草稿」，进页面 onMount 自动恢复；生成成功后静默存草稿。**追加：重新生成自动合并已有期初**——`read_existing_opening` 回读旧 opening 科目、`read_existing_fixed_rules` 回读旧折旧块，新值覆盖同名、其余保留（用户只填新增资产即可），权益差额重算；单测验证两批合并 3 科目/2 折旧规则正确。②**类型自定义**：投资/固定资产类别由 `<select>` 改 `<input list=…>` + `<datalist>`（基金/股票/债券/虚拟货币/贵金属/电子设备/家居家电/交通工具/房产/健身器械/办公设备/其他），用户可自己写。③**信用卡溢缴款**：`normBalances`、Go、Python 三处都不再自动把 Liabilities 正数转负——欠款填负数、溢缴款填正数，标签写明；实测 `Liabilities:信用卡=500` 原样入账。④**折旧去重**：第③步删除「大件资产」区块，只留工资/房租；固定资产统一在第②步（原价+购买日期+年限+残值）自动追溯历史折旧并生成 `fixed-assets-recurring.journal` 续折；Python 对建账日之后购买的资产跳过并提示。⑤**同步直接跳界面**：`syncUI` 成功后整页 `location.assign("/")`（排查发现该 fork 的 SPA 客户端导航到 `/` 会加载 chunk 但不挂载，整页跳转正常）；「去总览」与品牌 logo 加 `data-sveltekit-reload`。⑥验证：草稿恢复（日期/投资/固定资产/自定义行）、datalist 含健身器械、信用卡 500 原样提交、第③步无大件资产、同步后 `location.pathname=/` 且仪表盘渲染；合并单测通过；svelte-check 0 错；`paisa.exe` 22:40 重建。**未清库**（真实 journals/paisa.db 保持不动；你 22:19 已生成的期初与折旧规则会在下次重新生成时自动保留）。
